name: memento_mori_app
description: "Tactical Mesh Messenger"
publish_to: 'none'
version: 1.0.2+3

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- UI & UX ---
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.2
  animate_do: ^4.2.0
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  provider: ^6.1.1 # 🔥 БЫЛО ПОТЕРЯНО
  math_expressions: ^2.4.0
  qr_flutter: ^4.1.0

  # --- DI & Architecture ---
  get_it: ^7.7.0 # 🔥 БЫЛО ПОТЕРЯНО

  # --- Система & Hardening ---
  flutter_foreground_task: ^6.1.1
  wakelock_plus: ^1.2.8
  permission_handler: ^11.3.1
  device_info_plus: ^10.1.0
  package_info_plus: ^8.0.0
  shared_preferences: ^2.2.2
  sensors_plus: ^6.1.1 # 🔥 БЫЛО ПОТЕРЯНО

  # --- Сетевой стек (Mesh & Cloud) ---
  http: ^1.5.0
  web_socket_channel: ^3.0.3
  nearby_connections: ^4.0.0
  flutter_blue_plus: ^1.34.5
  geolocator: ^13.0.1
  geocoding: ^3.0.0 # Reverse geocoding для получения названия места
  flutter_ble_peripheral: ^2.0.1 # 🔥 БЫЛО ПОТЕРЯНО

  # --- Deep Tech (Sonar) ---
  sound_generator: ^0.0.14
  fftea: ^1.1.2
  just_audio: ^0.9.36

  # --- Безопасность ---
  cryptography: ^2.5.0
  crypto: ^3.0.3
  flutter_secure_storage: ^9.2.4
  sqflite: ^2.3.0
  path: ^1.8.3
  local_auth: ^2.1.6
  flutter_local_notifications: ^17.1.2
  uuid: ^4.4.0
  battery_plus: ^6.0.1
  synchronized: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1
  mocktail: ^1.0.0
  test: ^1.24.0
  fake_async: ^1.3.1

flutter:
  uses-material-design: true
  generate: true
  fonts:
    - family: Orbitron
      fonts:
        - asset: assets/fonts/Orbitron-VariableFont_wght.ttf
        - asset: assets/fonts/Orbitron-Bold.ttf
          weight: 700
    - family: RobotoMono
      fonts:
        - asset: assets/fonts/RobotoMono-VariableFont_wght.ttf
  assets:
    - assets/audio/
    - assets/fonts/