// Флаг для расширенного Identity Churn теста. При true в GossipManager
// используется сокращённый identity TTL (60 сек) только для теста.
// Устанавливается из UI (mesh_control_screen) перед запуском теста.

bool EXTENDED_IDENTITY_TEST_MODE = false;
