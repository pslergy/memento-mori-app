# Morri Backend — copy to .env and fill values

# Database (PostgreSQL). Prisma uses this for migrations and runtime.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"

# JWT secret (generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET="your-64-byte-hex-secret"

# Server
PORT=3000

# Optional: HTTPS (paths to PEM files). If set, server uses HTTPS instead of built-in certs.
# SSL_KEY_PATH=/etc/letsencrypt/live/yourdomain/privkey.pem
# SSL_CERT_PATH=/etc/letsencrypt/live/yourdomain/fullchain.pem

# Optional: Tor hidden service (see DEPLOYMENT.md). Morri listens on localhost; Tor forwards .onion to it.
# ENABLE_TOR_HIDDEN_SERVICE=1

# Optional: Firebase (push) — leave empty if not used
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccountKey.json
