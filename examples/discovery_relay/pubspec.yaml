name: discovery_relay_example
description: Example app demonstrating Discovery & Relay Pack
publish_to: 'none'

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  discovery_relay_pack:
    path: ../../sdk_products/discovery_relay_pack

dev_dependencies:
  flutter_test:
    sdk: flutter
